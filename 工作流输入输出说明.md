# 论文生成工作流 - 各阶段输入输出说明

## 工作流概览

工作流按顺序执行三个 Agent，每个 Agent 的输入和输出如下：

```
用户输入 (user_document)
    ↓
[阶段1] Paper Overview Agent
    ↓
[阶段2] LaTeX Paper Generator Agent
    ↓
[阶段3] Requirement Checklist Agent
    ↓
最终输出 (三个文件)
```

---

## 阶段1: Paper Overview Agent（论文概览生成）

### 📥 输入 (Input)

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `user_document` | `str` | ✅ | 用户提供的文档内容（原始输入） |
| `temperature` | `float` | ❌ | 温度参数（默认: 0.7） |
| `max_tokens` | `int` | ❌ | 最大token数（默认: 4000） |
| `model` | `str` | ❌ | 模型名称（可选，覆盖默认配置） |

### 📤 输出 (Output)

| 字段 | 类型 | 说明 |
|------|------|------|
| `file_name` | `str` | 生成的文件名，格式：`[Paper_Title]_[Paper_Type]_paper_overview.txt`<br>例如：`Deep_Learning_Method_paper_overview.txt` |
| `file_content` | `str` | 文件内容（纯文本），包含：<br>1. **Title**: 完整论文标题（英文）<br>2. **Paper Type**: Method 或 Survey<br>3. **Abstract**: 200-300字的摘要<br>4. **Research Content**: 研究内容描述<br>5. **Innovations**: 至少3个具体创新点<br>6. **Application Scenarios**: 应用场景 |
| `raw_response` | `str` | 原始响应（包含markdown格式） |
| `usage` | `dict` | Token使用情况：<br>`{prompt_tokens, completion_tokens, total_tokens}` |

### 📄 输出文件示例

**文件名**: `Deep_Learning_Method_paper_overview.txt`

**内容结构**:
```
Title: [Full paper title in English]

Paper Type: Method

Abstract:
[200-300 words abstract including background, problem, solution, results, significance]

Research Content:
[What problem, methods, scenarios, goals]

Innovations:
1. [First innovation - specific and verifiable]
2. [Second innovation - specific and verifiable]
3. [Third innovation - specific and verifiable]

Application Scenarios:
[Realistic application scenarios]
```

---

## 阶段2: LaTeX Paper Generator Agent（LaTeX论文生成）

### 📥 输入 (Input)

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `paper_overview` | `str` | ✅ | **从阶段1得到的文本内容**（`file_content`） |
| `user_info` | `str` | ❌ | 用户提供的额外信息（用于LaTeX生成） |
| `has_outline` | `bool` | ❌ | 用户是否提供了论文大纲（如果为True，会跳过生成） |
| `has_existing_tex` | `bool` | ❌ | 是否存在现有的.tex文件（如果为True，会跳过生成） |
| `temperature` | `float` | ❌ | 温度参数（默认: 0.7） |
| `max_tokens` | `int` | ❌ | 最大token数（默认: 16000） |
| `model` | `str` | ❌ | 模型名称（可选） |

### 📤 输出 (Output)

#### 情况1: 正常生成

| 字段 | 类型 | 说明 |
|------|------|------|
| `file_name` | `str` | 生成的文件名：`paper_framework.tex` |
| `file_content` | `str` | 完整的LaTeX论文内容，包含：<br>1. **Abstract** - 完整摘要（200-300字）<br>2. **Introduction** - 完整引言<br>3. **Related Work** - 相关工作综述<br>4. **Methodology** - 完整方法论（详细步骤）<br>5. **Experiments and Results** - 完整实验和结果（包含示例数据）<br>6. **Discussion** - 讨论（如适用）<br>7. **Conclusion** - 结论<br>8. **References** - 参考文献列表（10-30条） |
| `raw_response` | `str` | 原始响应（包含markdown格式） |
| `is_skipped` | `bool` | `False`（未跳过） |
| `skip_reason` | `None` | 无跳过原因 |
| `usage` | `dict` | Token使用情况 |

#### 情况2: 跳过生成

| 字段 | 类型 | 说明 |
|------|------|------|
| `file_name` | `None` | 未生成文件 |
| `file_content` | `None` | 无内容 |
| `raw_response` | `str` | 包含跳过信息的响应 |
| `is_skipped` | `bool` | `True`（已跳过） |
| `skip_reason` | `str` | 跳过原因：<br>- "User provided outline"<br>- "Existing .tex file exists" |
| `usage` | `dict` | Token使用情况（可能为空） |

### 📄 输出文件示例（正常生成时）

**文件名**: `paper_framework.tex`

**内容结构**:
```latex
\documentclass[conference]{IEEEtran}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{cite}
\usepackage{hyperref}
\usepackage{algorithm}
\usepackage{algorithmic}

\begin{document}
\title{[Paper Title]}
\author{[Authors]}
\maketitle

\begin{abstract}
[Complete abstract 200-300 words]
\end{abstract}

\section{Introduction}
[Complete introduction with full paragraphs...]

\section{Related Work}
[Complete related work review...]

\section{Methodology}
[Complete methodology section with detailed steps...]

\section{Experiments and Results}
[Complete experimental section with example data...]

\section{Conclusion}
[Complete conclusion...]

\begin{thebibliography}{99}
[Complete reference list...]
\end{thebibliography}

\end{document}
```

---

## 阶段3: Requirement Checklist Agent（需求清单生成）

### 📥 输入 (Input)

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `paper_overview` | `str` | ✅ | **从阶段1得到的文本内容**（`file_content`） |
| `latex_content` | `str` | ❌ | **从阶段2得到的LaTeX内容**（`file_content`）<br>**注意**: 只有当阶段2未跳过时才有值 |
| `user_original_input` | `str` | ❌ | **用户原始输入**（与阶段1的 `user_document` 相同）<br>**注意**: 只有当阶段2跳过时才使用此输入<br>**等价于**: 阶段1的输入 `user_document` |
| `temperature` | `float` | ❌ | 温度参数（默认: 0.7） |
| `max_tokens` | `int` | ❌ | 最大token数（默认: 4000） |
| `model` | `str` | ❌ | 模型名称（可选） |

### 📤 输出 (Output)

| 字段 | 类型 | 说明 |
|------|------|------|
| `file_name` | `str` | 生成的文件名：`requirements_checklist.md` |
| `file_content` | `str` | Markdown格式的需求清单，包含：<br><br>**第一大类: 画图需求**<br>1.1 算法图/Motivation图（正文用）<br>1.2 实验分析图（实验部分用）<br>1.3 Survey类型专用图表<br>1.4 表格<br><br>**第二大类: 文字需求**<br>2.1 第一部分: 摘要、引言<br>2.2 第二部分: 方法<br>2.3 第三部分: 实验分析 |
| `raw_response` | `str` | 原始响应（包含markdown格式） |
| `usage` | `dict` | Token使用情况 |

### 📄 输出文件示例

**文件名**: `requirements_checklist.md`

**内容结构**:
```markdown
# [Paper Title in English]

─────────────────────────────────────────────────────────────────────────────
📊 第一大类: 画图需求
─────────────────────────────────────────────────────────────────────────────

**1.1 算法图/Motivation图 (正文用):**
- [ ] 系统架构图 - 展示方法框架（放在Method章节，Method类型必需）
- [ ] 动机图 - 展示问题背景和动机（放在Introduction章节，可选）
- [ ] 算法流程图 - 展示关键算法步骤（放在Method章节，Method类型推荐）

**1.2 实验分析图 (实验部分用，Method类型为主):**
- [ ] 主实验结果对比图 - 与baseline对比（放在Experiments章节，Method类型必需）
- [ ] 消融实验结果图 - 展示各模块贡献（放在Experiments章节，Method类型推荐）

**1.3 Survey类型专用图表:**
- [ ] 方法分类对比图 - 展示不同方法类别（Survey类型推荐）
- [ ] 时间线图 - 展示领域发展历程（Survey类型推荐）

**1.4 表格:**
- [ ] 主实验结果表 - 对比各方法性能（Method类型必需）
- [ ] 消融实验结果表 - 各模块性能变化（Method类型推荐）

─────────────────────────────────────────────────────────────────────────────
✍️ 第二大类: 文字需求
─────────────────────────────────────────────────────────────────────────────

**2.1 第一部分: 摘要、引言**
- [ ] 摘要 (Abstract): 背景、问题、方法、结果、意义
- [ ] 引言 (Introduction): 动机、现有方法、局限、贡献、论文组织

**2.2 第二部分: 方法**
- [ ] 问题定义 - 数学符号定义输入、输出、目标
- [ ] 方法框架 - 整体流程描述（配合架构图）
- [ ] 核心模块 - 各模块详细说明和公式
- [ ] 算法伪代码 - 关键算法步骤

**2.3 第三部分: 实验分析**
- [ ] 实验设置 - 数据集、baseline、评估指标、实现细节
- [ ] 主实验结果 - 与baseline对比和分析
- [ ] 消融实验 - 各模块贡献分析
- [ ] 结果讨论 - 实验发现和原因分析
```

---

## 数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                    用户输入 (user_document)                      │
└────────────────────────────┬────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段1: Paper Overview Agent                                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 输入: user_document                                       │  │
│  │ 输出: paper_overview.txt                                  │  │
│  │      - file_name: "[Title]_[Type]_paper_overview.txt"    │  │
│  │      - file_content: 包含标题、类型、摘要、研究内容等     │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                              │
                              ▼
                    paper_overview (file_content)
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段2: LaTeX Paper Generator Agent                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 输入:                                                      │  │
│  │   - paper_overview (从阶段1)                              │  │
│  │   - user_info (可选)                                       │  │
│  │   - has_outline, has_existing_tex (控制是否跳过)          │  │
│  │                                                             │  │
│  │ 输出:                                                      │  │
│  │   情况A: 正常生成                                          │  │
│  │      - file_name: "paper_framework.tex"                    │  │
│  │      - file_content: 完整LaTeX论文                         │  │
│  │      - is_skipped: False                                   │  │
│  │                                                             │  │
│  │   情况B: 跳过生成                                          │  │
│  │      - file_name: None                                     │  │
│  │      - file_content: None                                  │  │
│  │      - is_skipped: True                                    │  │
│  │      - skip_reason: "User provided outline" 或            │  │
│  │                     "Existing .tex file exists"           │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
        情况A: 有LaTeX内容          情况B: 跳过
                │                           │
                ▼                           ▼
        latex_content              user_original_input
                │                           │
                └─────────────┬─────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段3: Requirement Checklist Agent                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 输入:                                                      │  │
│  │   - paper_overview (从阶段1，必需)                        │  │
│  │   - latex_content (从阶段2，如果阶段2未跳过)             │  │
│  │   - user_original_input (如果阶段2跳过，等于user_document) │  │
│  │                                                             │  │
│  │ 输出:                                                      │  │
│  │   - file_name: "requirements_checklist.md"                │  │
│  │   - file_content: Markdown格式的需求清单                  │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    最终输出 (三个文件)                            │
│  1. [Title]_[Type]_paper_overview.txt                          │
│  2. paper_framework.tex (如果未跳过)                            │
│  3. requirements_checklist.md                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 关键要点

### 1. 数据传递关系
- **阶段1 → 阶段2**: `paper_overview` 的 `file_content` 传递给阶段2作为 `paper_overview` 参数
- **阶段1 → 阶段3**: `paper_overview` 的 `file_content` 传递给阶段3作为 `paper_overview` 参数
- **阶段2 → 阶段3**: 
  - 如果阶段2未跳过：`latex_content` = 阶段2的 `file_content`
  - 如果阶段2跳过：`user_original_input` = 用户原始输入 `user_document`

### 2. 条件逻辑
- **阶段2的跳过条件**:
  - `has_outline == True` → 跳过（用户已提供大纲）
  - `has_existing_tex == True` → 跳过（已存在.tex文件）
- **阶段3的输入选择**:
  - 优先使用 `latex_content`（如果阶段2未跳过）
  - 否则使用 `user_original_input`（如果阶段2跳过）
  - **重要**: `user_original_input` 就是阶段1的输入 `user_document`（用户原始文档内容）

### 3. 文件保存
- 所有文件保存在同一个 `session` 文件夹中
- Session ID 格式：`session_YYYYMMDD_HHMMSS_uuid`
- 如果未提供 `session_id`，系统会自动生成

### 4. Token使用统计
- 每个阶段都会返回 `usage` 字典
- 工作流会累计所有阶段的Token使用情况，返回 `total_usage`

---

## 完整工作流响应结构

```json
{
  "session_id": "session_20250101_120000_abc123",
  "session_folder": "output/session_20250101_120000_abc123",
  "paper_overview": {
    "file_name": "Deep_Learning_Method_paper_overview.txt",
    "file_path": "output/session_.../Deep_Learning_Method_paper_overview.txt",
    "usage": {
      "prompt_tokens": 500,
      "completion_tokens": 1500,
      "total_tokens": 2000
    }
  },
  "latex_paper": {
    "file_name": "paper_framework.tex",
    "file_path": "output/session_.../paper_framework.tex",
    "is_skipped": false,
    "skip_reason": null,
    "usage": {
      "prompt_tokens": 2000,
      "completion_tokens": 8000,
      "total_tokens": 10000
    }
  },
  "requirement_checklist": {
    "file_name": "requirements_checklist.md",
    "file_path": "output/session_.../requirements_checklist.md",
    "usage": {
      "prompt_tokens": 800,
      "completion_tokens": 2000,
      "total_tokens": 2800
    }
  },
  "total_usage": {
    "prompt_tokens": 3300,
    "completion_tokens": 11500,
    "total_tokens": 14800
  }
}
```

